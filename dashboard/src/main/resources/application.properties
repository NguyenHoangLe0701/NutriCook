# Server Configuration
server.port=${PORT:8080}
spring.application.name=nutricook-dashboard

# ==========================================================
# DATABASE CONFIGURATION (MySQL Production)
# ==========================================================
spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Bảo Hibernate dùng đúng "ngôn ngữ" của MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Giữ nguyên cài đặt "update" (rất quan trọng)
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true



# # ==========================================================
# # DATABASE CONFIGURATION
# # Use H2 for development; switch to MySQL for production
# # ==========================================================
# spring.datasource.url=jdbc:h2:file:./data/nutricookdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.show-sql=false

# # Enable H2 console for debugging (optional)
# spring.h2.console.enabled=true

# ==========================================================
# GENERAL CONFIGURATION
# ==========================================================

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Static Resources
spring.web.resources.static-locations=classpath:/static/,file:./uploads/

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.thymeleaf.cache=false

# Server Error Pages
server.error.whitelabel.enabled=true
server.error.include-stacktrace=never
server.error.include-message=always

# Spring Security
spring.security.filter.order=10

# Firebase Configuration - Set to true to enable Firebase initialization
# NOTE: Replace serviceAccountKey.json with your actual Firebase Admin SDK key from Google Cloud Console
firebase.enabled=true